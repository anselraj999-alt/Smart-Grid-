# Only needed when the built site is NOT served from the same domain as the API
# (for `npm run dev` leave it empty - the Vite proxy handles /api).
# Copy to ".env" and set, for example:
# VITE_API_BASE=https://api.example.com
VITE_API_BASE=
