{
  "name": "smart-grid-backend",
  "version": "1.0.0",
  "description": "REST API for SmartGrid AI (auth + dashboard data). Zero dependencies - Node built-ins only.",
  "private": true,
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
